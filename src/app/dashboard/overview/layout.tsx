// Use the main overview component instead of parallel routes
import OverViewPage from '@/features/overview/components/overview';

export default function OverViewLayout() {
  return <OverViewPage />;
}
